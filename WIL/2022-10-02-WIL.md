# WIL

## Object 함수

```
// 지금까지 Array 함수를 주로 사용했지만, Object로 전달받은 값을 Key와 Value로 나누어 배열을 생성할 수도 있다.
// Object.keys | Object.values는 각각 키와 값에 해당하는 배열을 생성하며, 적용한 후에 배열함수를 체이닝해 그대로 사용할 수도 있다.
// ex) Object.keys(obj).map(() => { ...}) 등등

```
